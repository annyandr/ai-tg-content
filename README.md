
# 🤖 Medical SMM Automation

AI‑система для автоматизации медицинских Telegram‑каналов: генерация контента, проверка медицинской безопасности и умное планирование публикаций.

---

## 📋 Содержание

- [Возможности](#-возможности)
- [Архитектура](#-архитектура)
- [Быстрый старт](#-быстрый-старт)
- [Установка](#-установка)
- [Конфигурация](#-конфигурация)
- [Использование](#-использование)
- [Структура проекта](#-структура-проекта)
- [Работа через Docker](#-работа-через-docker)
- [Разработка](#-разработка)
- [FAQ](#-faq)

---

## ✨ Возможности

- AI‑генерация медицинских постов (Claude 3.5 через OpenRouter).
- Проверка медицинской безопасности и корректности формулировок.
- Планирование постов по расписанию (ежедневно в заданное время).
- Мгновенная публикация и отложенная публикация.
- Поддержка нескольких каналов и специализаций.
- Удобный Telegram‑интерфейс для врачей и редакторов.

Поддерживаемые специализации:

- 🍑 Гинекология (`@profgynecologist`)
- 👶 Педиатрия (`@profpediatrician`)
- 🩺 Эндокринология (`@profendocrinologist`)
- 🫀 Терапия (`@profphysician`)
- 🧴 Дерматология (`@profdermatologists`)

---

## 🏗 Архитектура

Высокоуровневая схема:

```text
Пользователь (врач/редактор)
        │
        ▼
Telegram Bot (aiogram)
        │  команды /start, /new_post, кнопки UI
        ▼
User Interface (handlers/user_interface.py)
        │
        ├─► GeneratorAgent (AI‑генерация поста)
        │
        ├─► SafetyAgent (проверка безопасности)
        │
        └─► MedicalTelegramBot + TaskQueue (постановка в очередь)
                    │
                    └─► Scheduler (APScheduler, фоновые задачи)
                            │
                            └─► Автопубликация постов в Telegram‑каналы
```

Основные компоненты:

- `src/agents/` — агенты генерации контента и проверки безопасности + промпты по специальностям.
- `src/telegram_bot/` — Telegram‑бот, модель задач, очередь задач, хендлеры интерфейса.
- `src/scheduler/` — планировщик публикаций на базе APScheduler.
- `src/services/openrouter.py` — клиент для OpenRouter API.
- `src/core/` — конфиг, логгер, общие исключения.
- `data/channels.json` — конфигурация каналов и специализаций.

---

## 🚀 Быстрый старт

### Требования

- Python 3.11+
- Аккаунт Telegram и созданный бот через @BotFather.
- API‑ключ OpenRouter.
- (Опционально) Docker и Docker Compose.

### 1. Клонирование репозитория

```bash
git clone https://github.com/annyandr/ai-tg-content.git
cd ai-tg-content
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 3. Настройка окружения

Создайте `.env` (можно на основе `.env.example`):

```bash
cp .env.example .env
```

Откройте `.env` и заполните обязательные поля:

```env
BOT_TOKEN=ваш_telegram_bot_token
OPENROUTER_API_KEY=ваш_openrouter_api_key
ADMIN_IDS=123456789
```

Остальные параметры можно оставить по умолчанию или отредактировать позже.

### 4. Запуск бота локально

```bash
python main.py
```

После запуска:

- Откройте вашего бота в Telegram.
- Отправьте `/start`.

---

## ⚙️ Конфигурация

Основной конфиг — `src/core/config.py`, значения берутся из `.env`.

Ключевые переменные окружения:

```env
# Telegram
BOT_TOKEN=123456789:ABCdefGHI...
ADMIN_IDS=123456789,987654321

# OpenRouter
OPENROUTER_API_KEY=sk-or-v1-...

# Database (для будущего расширения)
DATABASE_URL=sqlite+aiosqlite:///./data/database/medical_smm.db

# Scheduling
POSTING_TIMES=09:00,20:00
TIMEZONE=Europe/Moscow

# AI
DEFAULT_MODEL=anthropic/claude-3.5-sonnet
TEMPERATURE=0.7
MAX_TOKENS=2000

# Logging
LOG_LEVEL=INFO
```

Файл каналов: `data/channels.json` — описывает, в какие каналы и по каким специализациям публиковать.

Пример записи:

```json
{
  "id": "gynecology",
  "name": "Профессия – гинеколог",
  "username": "profgynecologist",
  "specialty": "гинекология",
  "emoji": "🍑",
  "link": "https://t.me/profgynecologist",
  "active": true
}
```

---

## 💻 Использование

### Telegram‑интерфейс

После `/start` бот покажет главное меню:

- «✨ Создать новый пост»
- «📋 Мои запланированные посты»
- «📊 Статистика»

Типичный сценарий:

1. Нажать **«Создать новый пост»**.
2. Выбрать специализацию (например, Гинекология).
3. Ввести тему/новость (например, «Новые рекомендации по ведению беременных с ГСД»).
4. Дождаться генерации (10–20 секунд).
5. Просмотреть пост и статус проверки безопасности.
6. Выбрать:
   - 🚀 «Опубликовать мгновенно» — моментальная публикация в канал.
   - ⏰ «Запланировать публикацию» — выбор времени (через 1 час, завтра в 9:00 и т.п.).
   - 🔄 «Сгенерировать заново» — получить альтернативную версию поста.

Дополнительные команды (можно добавить в админ‑хендлеры):

- `/scheduler` — список задач планировщика.
- `/queue` — ближайшие запланированные посты.
- `/stats` — статистика публикаций.

### Программный интерфейс

Через `MedicalTelegramBot` можно управлять публикациями программно.

Примеры:

```python
from datetime import datetime
from src.telegram_bot.bot import MedicalTelegramBot

bot = MedicalTelegramBot(bot_token="YOUR_TOKEN")
await bot.start()

# Мгновенная публикация
task_id = await bot.publish_now(
    channel_id="@profgynecologist",
    text="🍑 <b>Новый пост</b>\n\nКонтент..."
)

# Публикация по расписанию
task_id = await bot.schedule_post(
    channel_id="@profgynecologist",
    text="🍑 <b>Запланированный пост</b>",
    scheduled_time=datetime(2026, 2, 4, 9, 0, 0),
    buttons=[{"text": "Читать полностью", "url": "https://example.com"}]
)

# Отмена
await bot.cancel_post(task_id)

# Статус задачи
task = await bot.get_task_status(task_id)
print(task.status)

# Статистика
stats = bot.get_stats()
print(stats)
```

---

## 📂 Структура проекта

```text
medical-smm-automation/
│
├── main.py                  # Точка входа (запуск бота + планировщика)
├── requirements.txt
├── README.md
├── .env / .env.example
│
├── data/
│   ├── channels.json        # Конфиг каналов
│   ├── database/
│   │   └── medical_smm.db   # БД (для будущего расширения)
│   └── examples/            # Примеры постов
│
├── src/
│   ├── core/
│   │   ├── config.py        # Конфигурация
│   │   ├── logger.py        # Логгер
│   │   └── exceptions.py    # Кастомные исключения
│   │
│   ├── agents/
│   │   ├── base_agent.py
│   │   ├── generator_agent.py
│   │   ├── safety_agent.py
│   │   ├── generator_prompts.py
│   │   ├── safety_prompts.py
│   │   ├── dermatology_prompts.py
│   │   ├── gynecology_prompts.py
│   │   ├── endocrinology_prompts.py
│   │   ├── pediatrics_prompts.py
│   │   └── specialty_loader.py
│   │
│   ├── services/
│   │   └── openrouter.py    # Клиент OpenRouter
│   │
│   ├── telegram_bot/
│   │   ├── bot.py           # MedicalTelegramBot
│   │   ├── models.py        # PublishTask, TaskStatus, BotStats
│   │   ├── task_queue.py    # Очередь публикаций
│   │   └── handlers/
│   │       ├── user_interface.py
│   │       └── admin.py     # (опционально) админ‑команды
│   │
│   ├── scheduler/
│   │   ├── scheduler.py     # Обёртка над APScheduler
│   │   └── tasks.py         # Фоновые задачи (публикация, повторы, чистка)
│   │
│   └── utils/               # Вспомогательные утилиты (по мере роста проекта)
│       ├── formatters.py
│       └── helpers.py
│
└── logs/                    # Логи работы бота
```

---

## 🐳 Работа через Docker

В проекте предполагается использование двух файлов:

- `Dockerfile` — рецепт сборки образа (Python, зависимости, копирование кода, запуск `main.py`).
- `docker-compose.yml` — запуск сервиса `medical-smm-bot`:
  - Монтирование `./data` и `./logs` внутрь контейнера.
  - Подключение `.env` с токенами.
  - Режим `restart: unless-stopped`.

Общий сценарий работы:

```bash
# Первый запуск
docker compose up -d        # или docker-compose up -d

# Просмотр логов
docker compose logs -f

# Остановка
docker compose down

# Пересборка после изменения кода
docker compose up -d --build
```

Идея такая:

- Образ содержит Python, зависимости и код.
- Переменные окружения (BOT_TOKEN, OPENROUTER_API_KEY и др.) задаются через `.env`.
- Все данные (БД, конфиги, логи) живут вне контейнера, в папках `data/` и `logs/`.

---

## 🛠 Разработка

Рекомендованный воркфлоу:

1. Разработка и отладка локально с виртуальным окружением.
2. Добавление/коррекция промптов в `src/agents/*_prompts.py`.
3. Расширение Telegram‑интерфейса в `handlers/user_interface.py` и `handlers/admin.py`.
4. При необходимости — подключение реальной БД (SQLAlchemy) вместо in‑memory очереди.
5. Обновление `README.md` и `.env.example` при появлении новых настроек.

### Добавление новой медицинской специализации

1. Создайте файл промпта, например `src/agents/cardiology_prompts.py`.
2. Опишите там системный промпт и особенности канала.
3. Зарегистрируйте специализацию в `specialty_loader.py`.
4. Добавьте канал в `data/channels.json`.
5. Убедитесь, что он появляется в UI (список специализаций в `user_interface.py`).

---

## ❓ FAQ

**1. Бот не отвечает**

- Проверьте, что бот запущен (лог `🤖 MedicalTelegramBot инициализирован`).
- Убедитесь, что токен в `.env` верный.
- Проверьте, нет ли ошибки при старте в логах.

**2. Посты не публикуются в канал**

- Убедитесь, что бот добавлен в канал как администратор.
- У бота должны быть права «Публиковать сообщения».
- Проверьте, что `channel_id` вида `@username` указан корректно.

**3. Ошибка OpenRouter / нет генерации**

- Проверьте, что API‑ключ в `.env` актуален.
- Проверьте лимиты и баланс в личном кабинете OpenRouter.
- Убедитесь, что модель в `DEFAULT_MODEL` доступна вашему ключу.

**4. Посты не публикуются по расписанию**

- Убедитесь, что планировщик запущен (в логах есть сообщения от scheduler).
- Проверьте, что `POSTING_TIMES` и `TIMEZONE` корректны.
- Посмотрите `/scheduler` и `/queue` из админ‑бота.

---

## ✅ Краткая инструкция по запуску для демонстрации

1. Заполнить `.env` (BOT_TOKEN, OPENROUTER_API_KEY, ADMIN_IDS).
2. Установить зависимости: `pip install -r requirements.txt`.
3. Запустить: `python main.py`.
4. В Telegram:
   - Открыть бота.
   - `/start`.
   - Создать пост для одного из каналов.
   - Показать мгновенную публикацию и запланированную публикацию.

---
